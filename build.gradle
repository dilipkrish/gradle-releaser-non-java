buildscript {
    repositories {
        maven {
            url "http://nexus.containerstore.com/nexus/content/repositories/public/"
        }
        mavenCentral()
    }
    dependencies {
        classpath("com.containerstore.release:gradle-releaser:2.4.0")
    }
}

apply plugin: 'com.containerstore.release'
